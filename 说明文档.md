# Harness Demo 中文说明文档

## 一句话概括

这是一个 **Agent 编排框架的演示项目**：用自然语言接收用户需求，自动识别意图，再走完整的 Agent 循环（计划 → 执行 → 观察 → 反思），调度 Skill 和 MCP 工具，并带上记忆与可观测性，最后通过 CLI / FastAPI / Streamlit 三种方式使用。

业务场景偏 **财富管理 / 基金诊断**：解析基金报告、生成客户持仓总结、查询系统运行状态。

当前版本：`0.2.0`

---

## 它解决什么问题

传统做法是：写死一堆 if-else，或者把一切都塞进一次 LLM 调用。Harness 演示的是一套可扩展的「中间层」：

| 能力 | 作用 |
|------|------|
| 意图路由 | 把自然语言映射到具体业务能力 |
| Agent Loop | 按固定节点编排执行过程，可追踪、可回放 |
| Skill | 把业务能力封装成可插拔插件 |
| MCP Tool | 统一调用外部能力（文档解析、账户数据等） |
| 三层记忆 | 会话 / 用户 / 实体上下文 |
| 追踪与管理后台 | 看跑了什么、成功失败、意图分布 |

Demo 里大量用了 **Mock 数据与规则逻辑**（不必接真 LLM 也能跑通），同时预留了 Anthropic / OpenAI、LangFuse、Mem0 等真实接入开关。

---

## 整体架构

```
用户输入
   │
   ▼
意图路由（关键词 + 规则）
   │
   ▼
记忆检索（session / user / entity）
   │
   ▼
Plan（按意图生成步骤列表）
   │
   ▼
Execute ←→ Observe ←→ Reflect（可循环，有上限）
   │         Skill 调度
   │         MCP Tool 调用
   ▼
Output（汇总结果）+ 写入记忆 + 执行追踪
```

技术栈宣称：**DeepAgents + LangGraph + LangChain + LangFuse + Mem0**。  
核心循环实际由 **LangGraph `StateGraph`** 驱动；记忆、追踪在 demo 模式下多为内存实现，可按配置切到真实服务。

---

## 目录结构（你该看哪里）

```
harness-demo/
├── run_demo.py              # 一键入口：CLI / 演示 / API / Admin
├── harness/                 # 核心框架
│   ├── agent/loop.py        # Agent Loop（Plan→Execute→Observe→Reflect）
│   ├── intent/              # 意图模型、路由、持久化
│   ├── skills/              # Skill 基类、加载、注册、导入导出
│   ├── mcp/                 # MCP 管理器 / 注册表
│   ├── memory/              # 三层记忆 + Mem0 客户端封装
│   ├── observability/       # 指标、Span、Tracer（LangFuse 风格）
│   ├── db/                  # SQLAlchemy 模型与仓库
│   └── tracker.py           # 执行追踪（管理后台数据源）
├── skills/                  # 业务 Skill 包
│   ├── report_parser/       # 基金诊断报告解析
│   └── summary_generator/   # 客户总结生成
├── mcp_servers/
│   └── demo_tools.py        # 演示 MCP 工具（Mock）
├── config/                  # settings、意图 YAML、MCP YAML
├── api/                     # FastAPI 接口
├── admin/                   # Streamlit 管理后台
└── tests/                   # 单元测试
```

---

## 核心模块说明

### 1. Agent Loop（`harness/agent/loop.py`）

基于 LangGraph 的状态机，主流程节点：

1. **intent_route** — 识别意图，绑定 Skill  
2. **memory_retrieve** — 检索三层记忆  
3. **plan** — 按意图生成步骤列表（规则版「假 LLM」）  
4. **execute** — 真正干活：调 Skill / MCP；后续步骤主要记日志  
5. **observe** — 看结果是否成功、步骤是否走完  
6. **reflect** — 失败则跳过继续（简化策略）  
7. **output** — 汇总 `summary` 成最终回复  

入口类：`HarnessAgent.run(user_input, user_id)`，返回意图、置信度、计划、耗时、输出等。

### 2. 意图路由（`harness/intent/`）

策略是 **关键词打分 + 排除词降权 + 优先级加成**，不是大模型分类：

| 意图名 | 含义 | 绑定 Skill |
|--------|------|------------|
| `parse_report` | 解析基金诊断报告 | `report_parser` |
| `generate_summary` | 生成客户/持仓总结 | `summary_generator` |
| `query_status` | 查 Agent/Skill 运行统计 | 内置状态查询 |
| `manage_skill` | Skill 导入导出/启停 | 内置管理 |
| `unknown` | 未命中 | 引导用户换说法 |

配置可写在 `config/intents.yaml`，并同步到 SQLite。

### 3. Skill 体系（`harness/skills/` + `skills/`）

- 每个 Skill 有 `skill.yaml` 清单 + Python 实现类（继承 `BaseSkill`）  
- 框架可从目录自动发现、注册，并注入 MCP Manager  
- 支持启用/禁用、导入导出 `.harness-skill` 包、执行记录入库  

内置两个演示 Skill：

**report_parser**  
调用 MCP `parse_document` → 结构化提取基金名称、业绩、风险、持仓、诊断结论（当前为 Mock 提取）。

**summary_generator**  
调用 `get_account_analysis` + `get_holding_report` → 对齐数据 → 生成账户概况、持仓分析、风险提示、建议。

### 4. MCP 工具（`harness/mcp/` + `mcp_servers/demo_tools.py`）

统一注册与调用外部能力。Demo 提供 3 个本地 Tool：

| Tool | 作用 |
|------|------|
| `parse_document` | 模拟解析 PDF，返回基金诊断报告文本/表格 |
| `get_account_analysis` | 按客户 ID 返回账户分析（王总/李总/张总等 Mock） |
| `get_holding_report` | 返回私募持仓与产品表现 |

生产环境可通过 `config/mcp_servers.yaml` 接真实 MCP Server。

### 5. 记忆（`harness/memory/`）

三层结构（Mem0 风格，默认内存实现）：

- **session**：当前会话上下文  
- **user**：用户长期偏好/历史  
- **entity**：客户、基金、报告等业务实体  

检索用简单分词 + 关键词匹配，并对近期记忆加权。Agent 跑完会把「输入 + 输出摘要」写回 session 记忆。`MEM0_ENABLED=true` 时可走真实 Mem0。

### 6. 追踪与可观测（`harness/tracker.py` + `observability/`）

- `ExecutionTracker`：记录每次运行的步骤、MCP 调用、延迟、成败（管理后台主要数据源）  
- 可对接 LangFuse（`LANGFUSE_ENABLED`）做生产级 Trace  

### 7. 数据层（`harness/db/`）

默认 SQLite：`sqlite:///./data/harness.db`  

主要表：`agent_runs`、`agent_steps`、`skill_runs`、`skill_definitions`、`intent_definitions` 等，用于持久化运行与配置。

---

## 使用方式

```bash
pip install -r requirements.txt

# 最快：自动跑 4 个演示用例
python run_demo.py --demo

# 交互式 CLI
python run_demo.py

# 只起 API
python run_demo.py --api

# 只起管理后台
python run_demo.py --admin

# API + 管理后台一起起
python run_demo.py --all
```

| 服务 | 地址 |
|------|------|
| Streamlit 管理后台 | http://localhost:8501 |
| FastAPI Swagger | http://localhost:8000/docs |
| 健康检查 | http://localhost:8000/health |

CLI 可试的话术示例：

- 「帮我解析基金诊断报告」  
- 「生成王总账户的持仓总结」  
- 「查询系统运行状态」  

---

## API 能力概览

| 模块 | 路径前缀 | 能力 |
|------|----------|------|
| 对话 | `/chat` | 发起 Agent 对话 |
| Agent | `/admin/agents` | 运行日志、报表、详情 |
| Skill | `/admin/skills` | 列表、导入导出、启停 |
| 意图 | `/admin/intents` | CRUD、在线路由测试 |
| MCP | `/admin/mcp` | Server / Tool 列表与刷新 |
| 记忆 | `/admin/memory` | 浏览、搜索、删除 |

---

## 管理后台（Streamlit）

六个主要页面：

1. **Dashboard** — 运行统计、Skill 排行、意图分布  
2. **Agent 运行** — 日志、步骤回放  
3. **Skill 管理** — 列表、导入导出、启停  
4. **意图库** — CRUD、测试路由  
5. **对话测试** — 在线 Chat  
6. **记忆管理** — 三层记忆浏览  

---

## 一次请求实际发生了什么（举例）

用户说：「生成王总账户的持仓总结，分析一下风险」

1. 意图路由命中 `generate_summary`，绑定 `summary_generator`  
2. 检索记忆（可能为空）  
3. 计划大致为：取账户分析 → 取持仓 → 对齐 → 生成总结  
4. Execute 调用 MCP 拿 `wang_001` 的账户与持仓，再跑 Skill 生成总结文本  
5. Observe / Reflect 推进步骤  
6. Output 返回总结；Tracker 记一笔；Memory 写入本轮摘要  

---

## 配置要点（`.env`）

复制 `.env.example` 后按需修改：

- **LLM**：`LLM_PROVIDER` / `LLM_MODEL` / `LLM_API_KEY`  
- **LangFuse**：`LANGFUSE_ENABLED` + Key  
- **Mem0**：`MEM0_ENABLED`  
- **数据库**：默认 SQLite，可改 PostgreSQL  
- **Agent**：最大循环次数、超时等  

不配真实 Key 也能跑 Demo：计划与 Skill 提取多为本地规则 / Mock。

---

## 和「生产框架」的关系（读代码时要注意）

这是 **可运行的教学/演示仓库**，不是已经接到真实基金系统的生产实现：

- 意图路由是关键词，不是 LLM 分类  
- Plan / 字段提取大量是硬编码 Mock  
- MCP Demo Tool 返回写死的客户与报告数据  
- 记忆默认在内存里，进程重启会丢（除非开 Mem0 / 用 DB）  

但它把 **Agent 编排、Skill 插件、MCP 工具、记忆、追踪、API、管理后台** 这条链路完整串通了，适合作为二次开发脚手架：换成真实 LLM、真实 MCP、真实数据源即可。

---

## 总结

**Harness Demo = 一个面向财富管理场景的 Agent 编排演示框架。**  
用户用自然语言提问 → 框架路由意图 → LangGraph 循环调度 Skill 与 MCP → 带回记忆与执行轨迹 → 可用 CLI、HTTP API 或 Streamlit 后台操作与观察。
