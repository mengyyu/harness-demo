# Nixpacks build plan for Railway
# Specifies how to build the Python environment

[phases.setup]
nixPkgs = ["python312", "gcc"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r requirements.txt",
]

[variables]
PYTHONUNBUFFERED = "1"
