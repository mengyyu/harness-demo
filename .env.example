# LLM 配置（Demo 模式使用 Mock，无需真实 API Key）
LLM_PROVIDER=mock
LLM_MODEL=mock
LLM_API_KEY=not-needed

# 服务端口
API_PORT=8000
ADMIN_PORT=8501

# 记忆配置
MEMORY_MAX_SESSION=100
MEMORY_MAX_USER=500
MEMORY_MAX_ENTITY=1000

# Agent Loop 配置
AGENT_MAX_LOOPS=5
AGENT_TIMEOUT_SECONDS=300
